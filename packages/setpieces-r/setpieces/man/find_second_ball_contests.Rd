% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secondballs.R
\name{find_second_ball_contests}
\alias{find_second_ball_contests}
\title{Second-ball contests following a team's own set pieces}
\usage{
find_second_ball_contests(
  events,
  set_pieces,
  team_id,
  window_events = 5,
  window_minutes = 12/60,
  max_clean_passes_between = 1,
  final_third_start = FINAL_THIRD_START
)
}
\arguments{
\item{events}{`sorted_events(match)` for the same match `set_pieces`
was built from -- contest detection scans forward from each
delivery's `event_index` in this list.}

\item{set_pieces}{A list of set-piece event records (from
[extract_set_pieces()]) for the same match.}

\item{team_id}{The delivering team's contestantId.}

\item{window_events}{Max number of subsequent events to scan.}

\item{window_minutes}{Max time (in minutes) after the delivery to
keep scanning (default 12 seconds).}

\item{max_clean_passes_between}{Max number of clean (successful)
passes allowed between the first and second contested action
before treating the chain as broken.}

\item{final_third_start}{X-coordinate a delivery must reach to
qualify (default: start of the final third).}
}
\value{
A list of contest records, each with `delivery` (the
  originating set-piece event), `x`, `y`, `won` (logical), and
  `winner_player`.
}
\description{
Second-ball contests following a team's own set pieces
}
